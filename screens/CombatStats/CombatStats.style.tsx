import { StyleSheet } from "react-native";

export const CombatStatsStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    statsContainer: {
        justifyContent: "center",
        alignItems: "center",
    },
});